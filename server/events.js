const CONNECTION = "connection";
const DISCONNECT = "disconnect";

const UPDATE = "UPDATE";
const SAVE = "SAVE";

const BROADCAST = "BROADCAST";


module.exports = {
    CONNECTION,
    DISCONNECT,
    UPDATE,
    SAVE,
    BROADCAST
}